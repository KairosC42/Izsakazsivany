package rooms;

import entity.Item;
import rooms.Room;

import java.util.Random;

public class ItemRoom extends Room
{
    private Item item;

    private Random rand;

    /*
    Placeholder method!
     */

    @Override
    public void roomSpecificGen()
    {
        //create new random item
        //with a ~random image from item image pool
        //possibly correlate image (and name) with what stat the item changes?
    }
}
